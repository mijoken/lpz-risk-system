name: LPZ Prospective Self-Healing Batch Collector

on:
  workflow_dispatch:
  schedule:
    # GitHub cron is only a wake-up signal; slot time is deterministic UTC 15-minute grid.
    - cron: "7,22,37,52 * * * *"
  workflow_run:
    # Redundant GitHub-only wake-up path independent of this workflow's own cron.
    workflows: ["LPZ Public Production Cycle (O6)"]
    types: [completed]
  push:
    branches: [main]
    paths:
      - ".github/workflows/prospective-feature-collector.yml"
      - "scripts/o8_1_self_healing_batch_collector.py"
      - "scripts/o8_1_slot_replay_proof.py"
      - "scripts/build_prospective_feature_bundle.py"
      - "scripts/radar_*.py"
      - "scripts/build_parent_precursor_report.py"
      - "scripts/build_lpz_research_candidates.py"
      - "scripts/build_f3_research_batch.py"
      - "scripts/build_f4_observed_origin_join.py"
      - "scripts/build_f4_observed_origin_batch.py"
      - "tests/test_lpz_research_candidates.py"
      - "tests/test_f3_research_batch.py"
      - "tests/test_f4_observed_origin_join.py"
      - "tests/test_f4_observed_origin_batch.py"
      - "src/lpz_risk/**"
      - "tests/test_o8_1_self_healing_batch.py"

permissions:
  contents: read
  actions: read

concurrency:
  group: lpz-prospective-self-healing-batch
  cancel-in-progress: false

jobs:
  collect:
    if: github.event_name != 'workflow_run' || github.event.workflow_run.conclusion == 'success'
    runs-on: ubuntu-latest
    timeout-minutes: 45
    env:
      GH_TOKEN: ${{ github.token }}
    steps:
      - name: Checkout
        uses: actions/checkout@v7

      - name: Setup Python
        uses: actions/setup-python@v7
        with:
          python-version: "3.12"
          cache: "pip"

      - name: Install package
        run: |
          python -m pip install --upgrade pip
          python -m pip install -e . pytest

      - name: Verify ecCodes runtime
        run: python -m eccodes selfcheck

      - name: Run O8.1 collector unit tests
        run: python -m pytest -q tests/test_o8_1_self_healing_batch.py

      - name: Download recent lightweight batch indexes
        shell: bash
        run: |
          mkdir -p incoming/indexes
          gh run list \
            --repo "$GITHUB_REPOSITORY" \
            --workflow prospective-feature-collector.yml \
            --limit 30 \
            --json databaseId,conclusion,status \
            > /tmp/o81-runs.json
          python - <<'PY' > /tmp/o81-run-ids.txt
          import json
          rows=json.load(open('/tmp/o81-runs.json',encoding='utf-8'))
          for row in rows:
              if row.get('status') == 'completed' and row.get('conclusion') == 'success':
                  print(row['databaseId'])
          PY
          downloaded=0
          while read -r rid; do
            [ -z "$rid" ] && continue
            mkdir -p "incoming/indexes/$rid"
            if gh run download "$rid" \
              --repo "$GITHUB_REPOSITORY" \
              --pattern "prospective-batch-index-*" \
              --dir "incoming/indexes/$rid" >/dev/null 2>&1; then
              downloaded=$((downloaded+1))
            fi
          done < /tmp/o81-run-ids.txt
          echo "Prior lightweight batch indexes downloaded: $downloaded"

      - name: Build self-healing prospective batch
        shell: bash
        run: |
          python scripts/o8_1_self_healing_batch_collector.py \
            --output-root reports/prospective/o8_1_batch \
            --represented-root incoming/indexes \
            --run-id "${{ github.run_id }}" \
            --commit-sha "${{ github.sha }}" \
            --trigger-type "${{ github.event_name }}" \
            --catchup-horizon-minutes 120 \
            --settlement-lag-minutes 15 \
            --native-max-age-minutes 30 \
            --max-slots 8

      - name: Assert O8.1-C batch contract
        run: |
          python - <<'PY'
          import json
          p=json.load(open('reports/prospective/o8_1_batch/batch_manifest.json',encoding='utf-8'))
          assert p['risk_engine_allowed'] is False, p
          assert p['raw_radar_archived'] is False, p
          assert p['raw_grib_archived'] is False, p
          assert p['catchup_horizon_minutes'] <= 120, p
          assert p['settlement_lag_minutes'] == 15, p
          assert p['technical_failure_count'] == 0, p
          assert p['explicit_gap_count'] == 0, p
          assert p['deferred_due_run_cap_count'] == 0, p
          for row in p.get('slot_results', []):
              assert row['bundle_complete'] is True, row
              assert row['risk_engine_allowed'] is False, row
              assert row['collection_status'] in {
                  'COMPLETE_FEATURES',
                  'COMPLETE_NO_TRACKABLE_EVENT',
                  'COMPLETE_NO_EMBEDDED_GENESIS',
              }, row
              assert row['as_of_time_guard_pass'] is True, row
          print('PASS_PHASE2L_O8_1_C_SELF_HEALING_BATCH')
          print(json.dumps({
              'state': p['state'],
              'processed': p['requested_slot_count'],
              'native': p['native_slot_count'],
              'recovered': p['recovered_slot_count'],
              'represented_before': p['represented_slot_count_in_horizon'],
          }, indent=2))
          PY

      - name: Run F3 research adapter tests
        run: |
          python -m pytest -q tests/test_lpz_research_candidates.py tests/test_f3_research_batch.py

      - name: Build F3 research batch
        run: |
          python scripts/build_f3_research_batch.py \
            --batch-root reports/prospective/o8_1_batch

      - name: Assert F3 research-only contract
        run: |
          python - <<'PY'
          import json
          from pathlib import Path

          root = Path('reports/prospective/o8_1_batch')
          source = json.loads(
              (root / 'batch_manifest.json').read_text(encoding='utf-8')
          )
          result = json.loads(
              (root / 'f3_research' / 'manifest.json').read_text(encoding='utf-8')
          )

          assert result['risk_engine_allowed'] is False
          assert result['official_risk_output'] is False
          assert result['forecast_generated'] is False
          assert result['source_run_id'] == source['run_id']
          assert result['source_slot_count'] == source['requested_slot_count']
          assert len(result['slot_results']) == result['source_slot_count']

          print('PASS_F3_RESEARCH_ONLY_BATCH')
          PY

      - name: Run F4 observed-origin join tests
        run: |
          python -m pytest -q tests/test_f4_observed_origin_join.py tests/test_f4_observed_origin_batch.py

      - name: Build F4 observed-origin research batch
        run: |
          python scripts/build_f4_observed_origin_batch.py \
            --batch-root reports/prospective/o8_1_batch

      - name: Assert F4 observed-input-only contract
        run: |
          python - <<'PY'
          import json
          from pathlib import Path

          root = Path('reports/prospective/o8_1_batch')
          source = json.loads((root / 'batch_manifest.json').read_text(encoding='utf-8'))
          f3 = json.loads((root / 'f3_research' / 'manifest.json').read_text(encoding='utf-8'))
          f4 = json.loads((root / 'f4_observed_origins' / 'manifest.json').read_text(encoding='utf-8'))

          assert f4['product'] == 'F4_OBSERVED_ORIGIN_BATCH'
          assert f4['source_run_id'] == source['run_id'] == f3['source_run_id']
          assert f4['source_slot_count'] == source['requested_slot_count']
          assert f4['research_object_count'] == f3['research_object_count']
          assert f4['risk_engine_allowed'] is False
          assert f4['official_risk_output'] is False
          assert f4['forecast_generated'] is False
          assert len(f4['slot_results']) == f4['source_slot_count']
          for row in f4['slot_results']:
              assert row['risk_engine_allowed'] is False
              assert row['forecast_generated'] is False
              if row['join_status'] == 'OBSERVED_INPUT_JOINED':
                  obj = json.loads((root / row['output_file']).read_text(encoding='utf-8'))
                  assert obj['forecast_generated'] is False
                  assert obj['risk_engine_allowed'] is False
                  assert obj['current_origin_count'] <= obj['research_object_count']
                  assert all(o['forecast_probability'] is None
                             and o['lpz_classification'] is None
                             and o['forecast_valid_time_utc'] is None
                             for o in obj['objects'])
          print('PASS_F4_OBSERVED_INPUT_ONLY_BATCH')
          print(json.dumps({
              'slots': f4['source_slot_count'],
              'research_objects': f4['research_object_count'],
              'current_origins': f4['current_origin_count'],
          }, indent=2))
          PY

      - name: Write O8.1-C workflow summary
        if: always()
        run: |
          python - <<'PY' >> "$GITHUB_STEP_SUMMARY"
          import json
          from pathlib import Path
          p=Path('reports/prospective/o8_1_batch/batch_manifest.json')
          print('## LPZ O8.1-C Self-Healing Batch Collector')
          if not p.exists():
              print('- Batch manifest was not produced.')
              raise SystemExit(0)
          r=json.loads(p.read_text(encoding='utf-8'))
          print(f"- State: **{r['state']}**")
          print(f"- Trigger: **{r['trigger_type']}**")
          print(f"- Expected slots in 120m horizon: **{r['expected_slot_count_in_horizon']}**")
          print(f"- Already represented: **{r['represented_slot_count_in_horizon']}**")
          print(f"- Processed this run: **{r['requested_slot_count']}**")
          print(f"- Native / recovered: **{r['native_slot_count']} / {r['recovered_slot_count']}**")
          print(f"- Explicit gaps: **{r['explicit_gap_count']}**")
          print(f"- Technical failures: **{r['technical_failure_count']}**")
          print(f"- Deferred by run cap: **{r['deferred_due_run_cap_count']}**")
          print('- Risk Engine: **LOCKED**')
          PY

      - name: Upload full prospective batch
        if: always()
        uses: actions/upload-artifact@v6
        with:
          name: prospective-batch-${{ github.run_id }}
          path: reports/prospective/o8_1_batch/
          if-no-files-found: error
          retention-days: 3

      - name: Upload lightweight batch index
        if: always()
        uses: actions/upload-artifact@v6
        with:
          name: prospective-batch-index-${{ github.run_id }}
          path: reports/prospective/o8_1_batch/batch_manifest.json
          if-no-files-found: error
          retention-days: 10
